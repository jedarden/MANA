# MANA Sync Configuration
# Configure this to enable pattern synchronization across workspaces

enabled = false
interval_minutes = 60

[backend]
# Supported backends: Git, S3, PostgreSQL
type = "Git"
remote = ""
branch = "main"

[security]
sanitize_paths = true
redact_secrets = true
encrypt = true
visibility = "private"
